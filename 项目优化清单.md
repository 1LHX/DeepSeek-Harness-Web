# dsh-web 项目可优化内容清单

整理时间：2026-08-18
适用范围：dsh-panel v3.1（本轮 UI 圆角优化后的代码基线）
优先级：高 = 建议尽快处理 / 中 = 有空就做 / 低 = 锦上添花

---

## 一、缺陷类（高优先级）

### 1. DSH 检测结果可能读到旧缓存（真实 Bug）
`check-dsh.ps1` 会把结果写入 `run/dsh-check.txt` 且不清理；面板侧 `RunScriptWaitResult`（dsh-panel.cs:423）启动脚本后**立即**轮询该文件。而 PowerShell 从启动到执行脚本里的 `Remove-Item` 有约 1 秒延迟，面板第一次轮询就会读到上一轮的旧结果并直接返回——「重新检测」可能秒回缓存值，并未反映真实状态。
建议：面板侧在调用脚本前先删除结果文件，或脚本改用带时间戳/随机的结果文件名。

### 2. 设置页端口文案写死
`SettingsWindow.BuildUi` 中「监听端口」硬编码为 `"3080（dsh-web.config）"`（dsh-panel.cs:2346 附近），修改配置文件端口后显示不跟随。
建议：改为 `"Program.Port + \"（dsh-web.config）\"`。

### 3. stop-dsh.cmd 的端口解析不跳过注释
cmd 脚本用 `for /f tokens` 逐行读 `port=`，不跳过 `#` 注释行；C# 侧 `Config.Load` 会跳过。若注释里出现 `port=` 字样，两边行为不一致。
建议：cmd 侧加注释行过滤，与 C# 解析逻辑对齐。

### 4. PID 上限校验偏小
`CleanupStalePid` / `ReadPid` 用 `pid < 100000` 判定有效性，Windows 的 PID 在繁忙系统上可能超过 10 万，会把有效 pid 文件当无效删除。
建议：放宽到 `pid > 0` 即可（后续已有进程存在性校验兜底）。

---

## 二、死代码与冗余（中优先级）

| 位置 | 问题 | 建议 |
|---|---|---|
| `StatusLine` 类（dsh-panel.cs:2686 起约 80 行） | v1.2 遗留，全项目无实例化 | 删除 |
| 托盘菜单项 `miEmbed`「显示面板」（dsh-panel.cs:1218） | 创建并绑定事件，但从未加入 `trayMenu.Items` | 删除，或与「打开面板」合并 |
| `ShowLogWindow()`（dsh-panel.cs:1511） | 仅转发 `ShowLogWindowPublic()`，且无人调用 | 删除 |
| 「关于」里的版本信息与设置页「关于」分区 | 两处文案重复维护 | 抽成单一来源 |

---

## 三、体验与一致性（中优先级）

1. **托盘/系统菜单仍是直角系统样式**：WinForms 默认 ContextMenuStrip、窗口边框都是直角，与新的圆角自绘风格不统一。可用 ToolStripProfessionalRenderer 自定义圆角菜单（工作量中等）。
2. **开关是文字按钮「开/关」**：设置页 `MakeToggle` 用 SmallButton 模拟开关，观感偏旧。可画成真正的胶囊拨动开关（Track 圆角矩形 + 圆形滑块）。
3. **设置页「打开系统浏览器」不检查端口**：托盘菜单的同名项会先 `IsPortOpen()`，设置页直接打开，服务未运行时得到浏览器报错页。
4. **检测超时无反馈**：`CheckDshAsync` 30 秒超时后只当作「未安装」处理，用户分不清是真没装还是检测失败。建议超时给出明确提示。
5. **WebView2 缓存目录无清理入口**：`run/webview2-data` 会持续增长，可在设置页加「清除浏览器缓存」。

---

## 四、工程与构建（低优先级）

1. **构建产物散落在根目录**：`dsh-panel.exe` 与三个 WebView2 DLL 构建后复制在 exe 旁（CLR 探测限制所致）。可放 `bin/` 子目录并同步调整启动脚本与自启路径。
2. **版本号多点维护**：`AssemblyVersion`、文件头注释、README 版本史各自独立，升级易漏。建议只留程序集版本为唯一来源，README 动态引用。
3. **run-tests.cmd 覆盖面窄**：目前只做 GUI 启动冒烟，`--selftest` 才是完整链路但会停服务。可加一个不动现有服务的轻量参数组合，便于日常回归。
4. **自检的日志横幅校验硬编码** `dsh web` 字样，上游 DSH 输出格式一变即误报 WARN，可放宽为「日志非空即可」。

---

## 五、可选增强（低优先级）

1. 托盘气泡/活动日志支持英文或跟随系统语言（当前纯中文硬编码）。
2. 顶栏状态胶囊可加 tooltip 显示端口与运行时长。
3. `dsh-web.config` 目前仅一个 `port` 键，解析器却写了通用 key=value 框架，若无扩展计划可简化；若有计划（如日志保留大小、自启行为），建议在 README 预留键位说明。
4. 单实例激活用窗口标题 `FindWindowW`，标题若被本地化改掉会失效，可改用 `EnumWindows` + 进程 PID 匹配。

---

## 本轮已完成

- UI 圆角体系统一（Theme 常量：卡片 14 / 主按钮 11 / 小按钮 8），消除原先 5/9/10/12 混用
- 状态胶囊改全胶囊形、工具小按钮配色柔化（蓝灰系）、卡片投影与顶栏分隔线减淡
- Logo 与托盘图标圆角加大，整体更圆润
- 两次编译通过，产物已重新生成
